# ─────────────────────────────────────────────────────────────────────
#  NexaFlow frontend — environment template
#
#  Copy to .env.local and fill in. .env.local is gitignored; this file is
#  committed, so it must never contain a real secret.
#
#  Variables prefixed NEXT_PUBLIC_* are inlined into the browser bundle and
#  must be treated as public. Server-only variables below must never use that
#  prefix. Nothing secret belongs in this committed template — no private key,
#  mnemonic, provider credential, or Privy APP SECRET.
# ─────────────────────────────────────────────────────────────────────

# ── Wallet connection (Privy) ────────────────────────────────────────
#
# Get this from https://dashboard.privy.io → your app → Settings → Basics.
#
# The APP ID is public by design: it identifies your app to Privy from the
# browser and ships in the client bundle. It is NOT the app secret. The app
# secret is server-side only, is never prefixed NEXT_PUBLIC_, and must never
# appear in this project.
#
# In the Privy dashboard, also add your origins to Allowed domains, or login
# will fail in the browser with a domain error:
#   http://localhost:3000      (local development)
#   https://<your-domain>      (production)
NEXT_PUBLIC_PRIVY_APP_ID=

# ── Chain ────────────────────────────────────────────────────────────
# "bsc" for mainnet (chain 56). Anything else falls back to BSC testnet (97).
NEXT_PUBLIC_CHAIN=bsc

# ── Contracts ────────────────────────────────────────────────────────
# The membership contract. With this unset the app renders an explicit
# "not deployed" panel rather than querying 0x0 and showing zeroes as if
# they were real balances.
NEXT_PUBLIC_MEMBERSHIP_ADDRESS=

# Keep v5 until the V6 contract and frontend release are verified together.
NEXT_PUBLIC_MEMBERSHIP_VERSION=v5
# Required for v6: keccak256 of the deployed runtime code, including immutables.
# Derive after a verified deployment; never use the unlinked artifact hash.
NEXT_PUBLIC_V6_RUNTIME_HASH=

# First block containing this deployment. The network dashboard starts its
# event index here instead of scanning the full BSC chain. V4 and V5 have safe
# built-in fallbacks; set this for every future contract deployment.
NEXT_PUBLIC_MEMBERSHIP_DEPLOYMENT_BLOCK=

# The ERC-20 members pay fees in, pinned by ADDRESS rather than symbol.
# On BSC mainnet this is canonical USDT, which has 18 decimals.
NEXT_PUBLIC_PAYMENT_TOKEN_ADDRESS=0x55d398326f99059fF775485246999027B3197955

# ── RPC ──────────────────────────────────────────────────────────────
# Optional. Falls back to the chain's public endpoint if unset. Public RPCs
# rate-limit hard; use a dedicated provider before real traffic.
NEXT_PUBLIC_BSC_RPC_URL=
NEXT_PUBLIC_BSC_TESTNET_RPC_URL=

# RPC budget controls. Reads never poll in the background. The polling interval
# applies only while waiting for a submitted transaction receipt (or another
# explicitly watched operation). Values are clamped to safe ranges in code.
NEXT_PUBLIC_RPC_POLLING_INTERVAL_MS=8000
NEXT_PUBLIC_RPC_MULTICALL_WAIT_MS=40

# Alchemy fallback — used automatically if the primary RPC fails.
# Get a key at https://dashboard.alchemy.com (BNB Smart Chain Mainnet).
# https://bnb-mainnet.g.alchemy.com/v2/<YOUR_KEY>
NEXT_PUBLIC_BSC_RPC_URL_ALCHEMY=

# ── Server-side network index ────────────────────────────────────────
# Optional private RPC endpoint used only by /api/network while indexing
# MemberRegistered events. Prefer this in production so event scans do not
# consume the browser RPC allowance or reveal the provider URL to visitors.
BSC_RPC_URL=

# Optional event-query chunk size. The index automatically splits rejected
# ranges and caches finalized chunks, so historical blocks are not rescanned.
NETWORK_INDEX_CHUNK_SIZE=250000

# ── Optional serverless re-entry keeper (SERVER ONLY) ────────────────
# Never prefix any signing/storage/scheduler secret with NEXT_PUBLIC_.
# Disabled + dry-run by default; do not reuse an admin/deployer wallet.
KEEPER_ENABLED=false
KEEPER_DRY_RUN=true
CRON_SECRET=
KEEPER_WALLET_ADDRESS=
KEEPER_PRIVATE_KEY=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# Uses the private BSC_RPC_URL above. Exact deployed V6 runtime is verified.
KEEPER_MAX_GAS_PRICE_GWEI=0.1
KEEPER_MAX_GAS=2000000
KEEPER_BUDGET_BNB_24H=0.001
KEEPER_MIN_BALANCE_BNB=0.0001
# Read docs/queue-keeper.md before enabling. Persistent state must be initialized.
